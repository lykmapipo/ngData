'use strict';

describe('ngData:Module', function() {

    /// inject ngData $authProvider
    beforeEach(function() {
        module('ngData');
    });

    it('should be loaded and instantiated', function(done) {
        done();
    });
});